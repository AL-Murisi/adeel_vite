import {defineCliConfig} from 'sanity/cli'

export default defineCliConfig({
  api: {
    projectId: 'wfyqw3hs',
    dataset: 'test',
  },
  project: {
    basePath: '/studio',
  },
  /**
   * Enable auto-updates for studios.
   * Learn more at https://www.sanity.io/docs/cli#auto-updates
   */
  studioHost: 'adellllll',
  autoUpdates: true,
})
